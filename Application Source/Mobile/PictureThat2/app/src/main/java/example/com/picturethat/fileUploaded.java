package example.com.picturethat;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;


//gets a json file as a response and produce the results for the uploaded image.
public class fileUploaded extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_file_uploaded);
    }
}
