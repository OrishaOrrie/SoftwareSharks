import { UppyFiles, Meta } from './interfaces';
import * as StoreActions from './actions';
export interface State {
    plugins: object;
    files: UppyFiles<any>;
    currentUploads: object;
    capabilities: object;
    totalProgress: number;
    meta: Meta;
    info: object;
}
export declare function reducer(state: State, action: StoreActions.Actions): State;
