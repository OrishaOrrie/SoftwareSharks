Run the batch file using:
./runner "search term"

Run the individual image scraper Node program using:
node index.js "search term"

Run the python clean up program to delete invalid image files using:
python ".\check_images.py" "search term"

Please note that the "search term" is exactly how you would search for a term in Google, so excluding the "_" characters